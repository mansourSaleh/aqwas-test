const apiUrl = "https://wainnakel.com/api/v1/GenerateFS.php?uid=26.2716025,50.2017993&get_param=value"
//  If we are building a backend or in case we want to change the end point
// we just change it in one place

export default apiUrl